# Gunner Title Design

Rename the player-facing game title from `PHASER SHMUP` to `GUNNER` in both the plane-selection screen and browser tab. Internal state names, repository metadata, and persistent unlock keys remain unchanged to avoid unrelated migration risk.
